services:
  - type: web
    name: two-registers-web
    env: static
    buildCommand: npm run build
    staticPublishPath: build
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
