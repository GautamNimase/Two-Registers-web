services:
  - type: web
    name: two-registers-web
    env: static
    buildCommand: node build-robust.js
    staticPublishPath: build
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
